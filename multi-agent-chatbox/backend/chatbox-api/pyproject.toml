[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chatbox-api"
version = "0.1.0"
description = "Multi-agent chatbox API"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["app"]
